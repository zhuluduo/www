<!--
 clientWidth 获取盒子宽度
 let ingLength=imgList.children.length;//根据已知图片个数获取所有子集个数
 document.createElement("img")//创建元素
 document.getElementById("insert").innerHTML='<img src="new_banner.jpg" height="100" width="100" />';
 preview.setAttribute("src","images/slideshow.gif");
 preview.setAttribute("alt","图片正在加载");
 appendChild 兄弟元素之后插入  insertBefore之前
 firstElementChild 获取第一个一个子元素 兼容所有  lastElementChild

 s.childNodes;  //得到s的全部子节点
 s.parentNode;   //得到s的父节点
 s.nextSbiling;   //获得s的下一个兄弟节点
 s.previousSbiling;  //得到s的上一个兄弟节点
 s.firstChild;   //获得s的第一个子节点
 s.lastChile;   //获得s的最后一个子节点
 
 //动画兼容性写法
 element.style.webkitTransform = "";
 element.style.MozTransform = "";
 element.style.msTransform = "";
 element.style.OTransform = "";
 element.style.transform = "";
 -->