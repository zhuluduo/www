export const getDic = state=>{
    return state.dic;
}

export const getUser = state => state.user;

export const buyTotalcount = state => state.buytoTalcount;

export const messageCount = state => state.messageCount;
